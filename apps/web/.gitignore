node_modules
# Keep environment variables out of version control
.env

# Local HTTPS cert (mkcert) — machine-specific, never shared
.cert

/generated/prisma

# Agent-skill reference files auto-installed by `prisma init`, unrelated to this project
.agents
.claude/skills
.windsurf
skills-lock.json
